import React from 'react';
// Error element to show user something went wrong
const ErrorElement = () => {
	return (
		<div className="h-screen w-full flex items-center justify-center">
			<h1>Something went wrong</h1>
		</div>
	);
};

export default ErrorElement;
