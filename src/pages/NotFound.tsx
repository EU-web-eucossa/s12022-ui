import { Link } from 'react-router-dom';
import React from 'react';


const NotFound = () => {
	return (
		<div>
			NotFound <Link to={'/'}>Back to homepage</Link>
		</div>
	);
};

export default NotFound;
